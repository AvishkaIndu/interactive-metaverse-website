# Environment Configuration
NODE_ENV=development
PORT=5000

# Frontend URL (adjust based on your Vite dev server)
FRONTEND_URL=http://localhost:5173

# Email Configuration (Optional - for contact form and newsletter)
# Get these from your email provider (Gmail, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@metagame.io
ADMIN_EMAIL=admin@metagame.io

# Database Configuration (Optional - for production)
# MONGODB_URI=mongodb://localhost:27017/metagame
# or use MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/metagame

# JWT Secret (for user authentication)
JWT_SECRET=your-super-secret-jwt-key-here

# Other API Keys (add as needed)
# STRIPE_SECRET_KEY=sk_test_...
# DISCORD_BOT_TOKEN=...
# TWITCH_CLIENT_ID=...